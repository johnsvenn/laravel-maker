@if ($errors->any())
    <div class="alert alert-danger">
        <ul>
            @foreach ($errors->all() as $error)
                <li>{{ $error }}</li>
            @endforeach
        </ul>
    </div>
@endif

__METHOD.ADMIN.FORM__
<br>
<div class='form-group'>
 {!! Form::submit($submitButtonText, ['class' => 'btn btn-primary']) !!}
</div>